Use case: infographic-diagram
Asset type: rigorous English patent-PPT system architecture diagram, landscape 16:9
Reference role: use the earlier architecture image only as a structural reference; recreate all labels in English.

Title:
“GraphTask-R1 Curriculum v3: Certified Executable-Graph Self-Evolution”

Subtitle:
“Questioner generation → execution certification and deterministic admission → same-round Solver learning → next-round co-evolution”

Style:
Clean white canvas, high-end academic and patent presentation, flat vector-like blocks, orthogonal arrows, consistent thin strokes, restrained navy/slate/muted-teal palette, amber only for validation gates. No 3D, gradients, shadows, decorative background, people, robots, brains, clouds, logos, watermarks, or performance claims.

Composition:
A 16:9 left-to-right system architecture with a top curriculum band, left input panel, three horizontal center lanes, right round-state panel, bottom output bar, and one feedback arrow to the next round. Keep all labels fully visible and minimize arrow crossings.

Top band: “STAGED CURRICULUM”
1. “Stage 1 — Production: question + program format”
2. “Stage 2 — Grounding: entities, relations, handles, execution”
3. “Stage 3 — Frontier: conditional semantic difficulty”

Left panel: “INSTANCE-SCOPED INPUTS & CONSTRAINTS”
- Graph snapshot
- Relation catalogue
- Seed entities
- Base tasks
- Explicit random seed
Footer: “Replayable state • JSON-serializable”

Upper lane: “QUESTIONER PATH”
Separate Questioner adapter
→ One-shot {question, GraphScript v0.3}
→ Parse + type/schema validation
  ≤64 operations • SSA handles • relation whitelist
→ Bounded graph executor
  edge-visit budget • return budget • step trace
→ Task certification
  execution-derived gold • alignment • no answer leakage
→ Candidate task buffer

Middle lane: “EVALUATION & REWARD FEEDBACK”
Frozen Solver evaluation
→ Interface readiness = parse rate × execution rate given parse
→ Semantic success measured only on executable samples
→ Staged dense reward for Questioner
Gate label:
“Effective frontier weight = configured weight × interface readiness”

Lower lane: “ARCHIVE & SOLVER PATH”
Candidate task buffer
→ Deterministic admission
  difficulty window • structural novelty • textual novelty • structured rejection reason
→ Persistent task archive
→ Rebuild Solver dataset in the same round
  base + historical + newly admitted tasks
  easy-to-hard curriculum + easy replay
→ Separate Solver adapter — GRPO update

Right panel: “ROUND STATE”
- Updated Questioner adapter
- Updated Solver adapter
Feedback arrow: “NEXT ROUND”

Bottom bar:
“OUTPUTS: verified graph-QA model • replayable task certificates and execution traces • controlled training-task archive”

Typography:
Highly legible English sans-serif with consistent title, group-header, block-label, and note hierarchy. Use no Chinese characters. Do not add or omit functional modules. Ensure all arrow directions are technically correct.

Flow correction for the recommended v2 image:
- Certified candidate tasks feed frozen-Solver evaluation and deterministic admission.
- The candidate-task buffer does not directly update the Questioner adapter.
- Staged reward feedback is the direct update source for the updated Questioner adapter.
- Solver GRPO update is the direct update source for the updated Solver adapter.
