# GraphTask-R1 runtime dependencies.
#
# Install torch, verl, SGLang, Ray, FlashAttention and the matching CUDA stack
# separately on the training server. They are intentionally not managed here,
# so this file cannot replace a working GPU environment.
aiohttp>=3.10,<4
pyarrow>=15,<22
pydantic>=2.7,<3
PyYAML>=6,<7
