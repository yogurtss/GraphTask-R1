# GraphTask-R1 runtime dependencies.
#
# Install torch and ms-swift in the dedicated training environment. GPU packages
# remain externally managed so this file cannot replace a working CUDA environment.
aiohttp>=3.10,<4
pyarrow>=15,<22
pydantic>=2.7,<3
PyYAML>=6,<7
