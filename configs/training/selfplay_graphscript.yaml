model_path: Qwen/Qwen3-4B-Instruct-2507
initial_adapter: ${INITIAL_ADAPTER}
base_tasks: ${BASE_TASKS}
val_data: ${VAL_DATA}
questioner_seeds: ${QUESTIONER_SEEDS}
graph_snapshot: freebase-v1
rounds: 3
solver_episodes: 256
opponent_samples: 8
base_ratio: 0.35
archive_ratio: 0.35
new_ratio: 0.30
seed: 42
actor_gpus: "0,1"
opponent_gpus: "2,3"
sglang_port: 30000
opponent_port: 18080
train_script: scripts/train_verl.sh
sglang_start_timeout_s: 300
interaction_mode: graphscript
relation_catalog: ${RELATION_CATALOG}
relation_catalogs:
  kqapro-v1: ${KQAPRO_RELATION_CATALOG}
  freebase-v1: ${RELATION_CATALOG}
max_follow_limit: 100
max_edge_visits: 200
max_returned_entities: 1000
program_profile: graphscript_v0_1
